import React, {PropTypes} from "react";
import "../App";

function Profile() {
  return (
    <div>
      <h1>Profile for </h1>
      </div>
  );
}

// Profile.propTypes = {
//   username: PropTypes.string.isRequired
// };

export default Profile;
